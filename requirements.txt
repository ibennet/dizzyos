# Mac / development dependencies.
# On the Raspberry Pi, install the REAL driver instead of the emulator — see README
# (Adafruit's rgb-matrix.sh builds hzeller/rpi-rgb-led-matrix with Python bindings).
RGBMatrixEmulator>=0.11
Pillow>=10.1
PyYAML>=6.0
qrcode>=7.4
